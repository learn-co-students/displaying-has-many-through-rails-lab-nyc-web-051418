<h1> <%= @doctor.name %></h1>
<h2> <%= @doctor.department %></h2>

<!--
  displays the doctor's name, department, and appointments,
   with each appointment's date, time, and patient name
   (linking to the patient's show page).
-->

<h3>Appointments:</h3>
  <% @doctor.appointments.each do |app| %>
    <%= link_to app.patient.name, patient_path(app.patient) %>
    at <%= link_to app.appointment_datetime.strftime("%B %-d, %Y at %H:%M"), appointment_path(app.id) %><br>
  <% end %>
