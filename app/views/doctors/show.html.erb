<!-- Create a Doctor#show page that displays the doctor's name, department, and appointments, with each appointment's date, time, and patient name (linking to the patient's show page). January 12, 2016 at 3:00 -->

<h1><%= @doctor.name %></h1>
<h2>Department: <%= @doctor.department %></h2>

<h2>Appointments</h2>
<ol>
  <% @doctor.appointments.each do |appt| %>
    <li>
      <%= appt.appointment_datetime.strftime("%B %-d, %Y at %R")%>
      <%= link_to "#{appt.patient.name}", patient_path %>
    </li>
    <br>
  <% end %>
</ol>
