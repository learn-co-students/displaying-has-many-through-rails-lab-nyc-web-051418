<h1>Doctors</h1>

<!--
  displays each doctor's name, a link to their show page,
    and a count of their patients.
-->
<ul>
  <% @doctors.each do |doctor| %>
    <li><%= link_to doctor.name, doctor_path(doctor) %> <%= doctor.patient_count%></li>
  <% end %>
</ul>
